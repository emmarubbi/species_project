#include "species.hpp"

namespace spcs {
    namespace input {
        double ask_for_duration();
        double ask_for_cout_step();
        d_pair ask_for_initial_conditions();
        sim_parameters ask_for_parameters();
    }
}